apply from: "${rootDir}/gradle/java.gradle"
apply from: "${rootDir}/gradle/junit5.gradle"
apply from: "${rootDir}/gradle/allure.gradle"

dependencies {
    testCompile project(":sut")
    testCompile libs.logging
}