apply plugin: 'java'
apply plugin: 'idea'

sourceSets {
    test {
        java {
            srcDir 'src/test/java'
        }
        resources {
            srcDir 'src/test/resources'
        }
    }
}

sourceCompatibility = 1.8
targetCompatibility = 1.8

task dist {
    dependsOn 'build'
}
